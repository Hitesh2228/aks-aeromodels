---
import Header from '../components/Header.astro';
import CartDrawer from '../components/CartDrawer.astro';
import Footer from '../components/Footer.astro';
import { PRODUCTS, CATEGORIES } from '../data/products';
import '../styles/global.css';
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Shop All Products | SKYNODESUAV</title>
  <meta name="description" content="Browse 43+ premium contest grade RC engines, Futaba transmitters, Seagull aeromodels, balsa wood sheets, and flight accessories." />
</head>
<body class="bellavita-shop-body">

  <Header />

  <!-- Breadcrumbs -->
  <nav class="breadcrumb-strip" aria-label="Breadcrumb">
    <div class="container">
      <a href="/">Home</a> <span>›</span> <span class="active">Shop All Products</span>
    </div>
  </nav>

  <main class="container pdf-shop-container reveal-on-scroll">
    
    <!-- Top Title & Sort Action Row -->
    <div class="shop-title-action-row">
      <div>
        <h1 class="shop-pdf-title">Shop All Products</h1>
        <p class="shop-pdf-count">{PRODUCTS.length} items available in stock</p>
      </div>

      <div class="shop-right-actions">
        <div class="sort-select-wrap">
          <label for="sort-select" class="sort-lbl">Sort By:</label>
          <select id="sort-select" class="sort-dropdown">
            <option value="featured">Featured & Bestsellers</option>
            <option value="price-low">Price: Low to High</option>
            <option value="price-high">Price: High to Low</option>
            <option value="rating">Highest Rated</option>
          </select>
        </div>
      </div>
    </div>

    <!-- Category Filter Pills Strip (Exact PDF Page 1 Row) -->
    <div class="category-pills-strip">
      <button class="cat-pill-btn active" data-cat="all">All</button>
      {CATEGORIES.map(cat => (
        <button class="cat-pill-btn" data-cat={cat.id}>{cat.label}</button>
      ))}
    </div>

    <!-- Explore With Categories Banner Grid (Exact PDF Page 2 Box) -->
    <div class="explore-categories-box">
      <h3 class="explore-heading">Explore With Categories</h3>
      <div class="explore-grid">
        <div class="explore-card" data-cat="engine">
          <span class="exp-icon">⚡</span>
          <strong>Engines</strong>
        </div>
        <div class="explore-card" data-cat="radio-receiver">
          <span class="exp-icon">📡</span>
          <strong>Radios & Receivers</strong>
        </div>
        <div class="explore-card" data-cat="aeromodels">
          <span class="exp-icon">✈️</span>
          <strong>Seagull Models</strong>
        </div>
        <div class="explore-card" data-cat="balsa-wood">
          <span class="exp-icon">🪵</span>
          <strong>Balsa Wood</strong>
        </div>
        <div class="explore-card" data-cat="accessories">
          <span class="exp-icon">🛠️</span>
          <strong>Accessories</strong>
        </div>
      </div>
    </div>

    <!-- 5-Column Product Cards Grid (Exact PDF Style & Perfectly Aligned Add To Cart Buttons) -->
    <div class="pdf-shop-grid-5" id="catalog-products-grid">
      {PRODUCTS.map(product => (
        <article 
          class="pdf-shop-card catalog-item" 
          data-category={product.category} 
          data-price={product.price} 
          data-rating={product.rating}
          data-bestseller={product.isBestseller ? 'true' : 'false'}
        >
          <!-- Image Stage -->
          <div class="card-img-stage">
            {product.isBestseller && <span class="badge-tag bestseller">BESTSELLER</span>}
            {product.isNewArrival && <span class="badge-tag new">NEW</span>}
            {product.discountBadge && <span class="badge-tag offer">{product.discountBadge}</span>}
            <a href={`/product/${product.id}`}>
              <img src={product.image} alt={product.name} class="product-thumb-img" loading="lazy" />
            </a>
          </div>

          <!-- Card Content Body -->
          <div class="card-info-body">
            <div class="card-rating-line">
              <span class="star-badge-yellow">★ {product.rating}</span>
              <span class="rating-count-lbl">({product.reviewsCount})</span>
            </div>

            <div class="card-cat-label">{product.categoryLabel.toUpperCase()}</div>
            <h2 class="card-title-heading">
              <a href={`/product/${product.id}`}>{product.name}</a>
            </h2>

            <div class="saving-green-tag">
              (Saving ₹{(product.originalPrice - product.price).toLocaleString('en-IN')})
            </div>

            <div class="price-row-line">
              <span class="price-now">₹{product.price.toLocaleString('en-IN')}.00</span>
              <span class="price-was">₹{product.originalPrice.toLocaleString('en-IN')}.00</span>
            </div>

            <!-- ADD TO CART Button (100% Horizontally Aligned in 1 Line Across Grid Row) -->
            <button class="pdf-cart-btn-black add-to-cart-btn" data-id={product.id}>
              Add To Cart
            </button>
          </div>
        </article>
      ))}
    </div>

  </main>

  <CartDrawer />
  <Footer />

  <style>
    :global(body.bellavita-shop-body) {
      background-color: #fbf9f5 !important;
      color: #1a1a1a !important;
    }

    /* Minimal Breadcrumb Strip */
    .breadcrumb-strip {
      background: transparent;
      padding: 1.25rem 0 0.5rem;
      font-size: 0.78rem;
      color: #777777;
    }
    .breadcrumb-strip a { color: #666666; text-decoration: none; }
    .breadcrumb-strip a:hover { color: #111111; }
    .breadcrumb-strip span { margin: 0 0.35rem; color: #aaaaaa; }
    .breadcrumb-strip .active { color: #111111; font-weight: 600; }

    /* Shop Container with Proper PDF Left/Right Margins */
    .pdf-shop-container {
      max-width: 1280px !important;
      margin: 0 auto !important;
      padding: 1rem 2rem 6rem !important;
    }

    /* Title & Sort Action Row */
    .shop-title-action-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 1.75rem;
    }
    .shop-pdf-title {
      font-family: var(--font-display);
      font-size: 1.8rem;
      font-weight: 600;
      color: #1a1a1a;
      letter-spacing: -0.01em;
    }
    .shop-pdf-count {
      font-size: 0.82rem;
      color: #666666;
    }
    .sort-select-wrap {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      font-size: 0.85rem;
    }
    .sort-lbl { color: #666666; font-weight: 600; }
    .sort-dropdown {
      background: #ffffff;
      border: 1px solid #d6cfc2;
      border-radius: 4px;
      padding: 0.4rem 0.75rem;
      font-size: 0.82rem;
      color: #1a1a1a;
      outline: none;
      cursor: pointer;
    }

    /* Category Filter Pills Strip (Exact PDF Page 1) */
    .category-pills-strip {
      display: flex;
      gap: 0.65rem;
      overflow-x: auto;
      padding-bottom: 0.5rem;
      margin-bottom: 2.25rem;
    }
    .cat-pill-btn {
      padding: 0.5rem 1.15rem;
      font-size: 0.82rem;
      font-weight: 600;
      border-radius: 4px;
      border: 1px solid #d6cfc2;
      background: #ffffff;
      color: #1a1a1a;
      cursor: pointer;
      white-space: nowrap;
      transition: var(--transition-fast);
    }
    .cat-pill-btn:hover, .cat-pill-btn.active {
      background: #111111;
      color: #ffffff;
      border-color: #111111;
    }

    /* Explore With Categories Banner Grid (Exact PDF Page 2) */
    .explore-categories-box {
      background: #f8f5ee;
      border: 1px solid #e6e0d5;
      border-radius: 8px;
      padding: 1.5rem 2rem;
      margin-bottom: 2.5rem;
    }
    .explore-heading {
      font-family: var(--font-display);
      font-size: 1.05rem;
      font-weight: 700;
      color: #1a1a1a;
      margin-bottom: 1.25rem;
      text-align: center;
    }
    .explore-grid {
      display: grid;
      grid-template-columns: repeat(5, 1fr);
      gap: 1.25rem;
    }
    .explore-card {
      background: #ffffff;
      border: 1px solid #e6e0d5;
      border-radius: 6px;
      padding: 1rem;
      text-align: center;
      cursor: pointer;
      transition: var(--transition-fast);
    }
    .explore-card:hover {
      transform: translateY(-2px);
      border-color: #111111;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
    }
    .exp-icon { font-size: 1.5rem; display: block; margin-bottom: 0.35rem; }
    .explore-card strong { font-size: 0.8rem; color: #1a1a1a; }

    /* 5-Column Product Grid with 100% Horizontally Aligned Add To Cart Buttons */
    .pdf-shop-grid-5 {
      display: grid;
      grid-template-columns: repeat(5, 1fr);
      gap: 1.25rem;
    }
    .pdf-shop-card {
      background: #ffffff;
      border: 1px solid #e6e0d5;
      border-radius: var(--radius-sm);
      overflow: hidden;
      display: flex;
      flex-direction: column;
      height: 100%;
      transition: var(--transition-smooth);
    }
    .pdf-shop-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.08);
      border-color: #111111;
    }

    .card-img-stage {
      position: relative;
      padding-top: 85%;
      background: #f7f4ed;
    }
    .product-thumb-img {
      position: absolute;
      inset: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
    .badge-tag {
      position: absolute;
      top: 0.6rem;
      font-size: 0.58rem;
      font-weight: 800;
      padding: 0.2rem 0.5rem;
      border-radius: 4px;
      z-index: 2;
    }
    .badge-tag.bestseller { left: 0.6rem; background: #ea580c; color: #fff; }
    .badge-tag.new { left: 0.6rem; background: #2563eb; color: #fff; }
    .badge-tag.offer { right: 0.6rem; background: #059669; color: #fff; }

    /* Card Content Body (Flex Column for Perfectly Aligned Add to Cart Buttons) */
    .card-info-body {
      padding: 1rem;
      display: flex;
      flex-direction: column;
      flex-grow: 1;
    }
    .card-rating-line {
      display: flex;
      align-items: center;
      gap: 0.35rem;
      margin-bottom: 0.35rem;
    }
    .star-badge-yellow {
      background: #fef3c7;
      color: #92400e;
      font-size: 0.72rem;
      font-weight: 800;
      padding: 0.15rem 0.4rem;
      border-radius: 3px;
    }
    .rating-count-lbl { font-size: 0.72rem; color: #777777; }

    .card-cat-label { font-size: 0.65rem; color: #777777; font-weight: 700; margin-bottom: 0.25rem; }
    .card-title-heading {
      font-size: 0.88rem;
      font-weight: 600;
      color: #1a1a1a;
      line-height: 1.35;
      margin-bottom: 0.4rem;
      height: 2.5rem;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }
    .card-title-heading a:hover { color: #3b0811; }

    .saving-green-tag {
      font-size: 0.75rem;
      font-weight: 700;
      color: #059669;
      margin-bottom: 0.4rem;
    }

    .price-row-line {
      display: flex;
      align-items: baseline;
      gap: 0.5rem;
      margin-bottom: 0.85rem;
    }
    .price-now { font-size: 1.05rem; font-weight: 700; color: #1a1a1a; }
    .price-was { font-size: 0.8rem; color: #888888; text-decoration: line-through; }

    /* ADD TO CART Button - 100% Horizontally Aligned in 1 Straight Line Across Grid Row */
    .pdf-cart-btn-black {
      margin-top: auto !important;
      width: 100%;
      background: #111111;
      color: #ffffff;
      font-weight: 700;
      font-size: 0.8rem;
      padding: 0.75rem;
      border-radius: 4px;
      border: 1px solid #111111;
      cursor: pointer;
      text-transform: capitalize;
    }

    @media (max-width: 1100px) {
      .pdf-shop-grid-5 { grid-template-columns: repeat(3, 1fr); }
      .explore-grid { grid-template-columns: repeat(3, 1fr); }
    }
    @media (max-width: 700px) {
      .pdf-shop-grid-5 { grid-template-columns: repeat(2, 1fr); }
      .explore-grid { grid-template-columns: repeat(2, 1fr); }
      .pdf-shop-container { padding: 1rem 1rem 4rem !important; }
    }
  </style>

  <script>
    import { addToCart } from '../lib/cartStore';

    document.addEventListener('DOMContentLoaded', () => {
      const pills = document.querySelectorAll('.cat-pill-btn');
      const exploreCards = document.querySelectorAll('.explore-card');
      const products = document.querySelectorAll('.catalog-item');
      const sortSelect = document.getElementById('sort-select') as HTMLSelectElement;
      const grid = document.getElementById('catalog-products-grid');

      function filterCategory(cat: string) {
        pills.forEach(p => {
          if (p.getAttribute('data-cat') === cat) p.classList.add('active');
          else p.classList.remove('active');
        });

        products.forEach(prod => {
          const itemCat = prod.getAttribute('data-category');
          if (cat === 'all' || itemCat === cat) {
            (prod as HTMLElement).style.display = 'flex';
          } else {
            (prod as HTMLElement).style.display = 'none';
          }
        });
      }

      pills.forEach(p => {
        p.addEventListener('click', () => {
          const cat = p.getAttribute('data-cat');
          if (cat) filterCategory(cat);
        });
      });

      exploreCards.forEach(c => {
        c.addEventListener('click', () => {
          const cat = c.getAttribute('data-cat');
          if (cat) filterCategory(cat);
        });
      });

      // Filter by URL Query parameter (e.g. ?category=engine)
      const params = new URLSearchParams(window.location.search);
      const urlCat = params.get('category');
      if (urlCat) filterCategory(urlCat);

      // Dynamic Sorting Handler
      sortSelect?.addEventListener('change', () => {
        const val = sortSelect.value;
        const itemsArr = Array.from(products);

        itemsArr.sort((a, b) => {
          const priceA = parseFloat(a.getAttribute('data-price') || '0');
          const priceB = parseFloat(b.getAttribute('data-price') || '0');
          const ratingA = parseFloat(a.getAttribute('data-rating') || '0');
          const ratingB = parseFloat(b.getAttribute('data-rating') || '0');
          const bestA = a.getAttribute('data-bestseller') === 'true' ? 1 : 0;
          const bestB = b.getAttribute('data-bestseller') === 'true' ? 1 : 0;

          if (val === 'price-low') return priceA - priceB;
          if (val === 'price-high') return priceB - priceA;
          if (val === 'rating') return ratingB - ratingA;
          return bestB - bestA;
        });

        itemsArr.forEach(item => grid?.appendChild(item));
      });

      // Generic Add To Cart Listener
      document.querySelectorAll('.add-to-cart-btn').forEach(btn => {
        btn.addEventListener('click', (e) => {
          const id = (e.currentTarget as HTMLElement).getAttribute('data-id');
          if (id) addToCart(id, 1);
        });
      });
    });
  </script>

</body>
</html>
